{
  "name": "stj-bus-tracker-backend",
  "version": "1.0.0",
  "description": "Backend API for St. John's Bus Tracker",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm ci && npm rebuild better-sqlite3 --build-from-source",
    "postinstall": "npm rebuild better-sqlite3 --build-from-source || echo 'Rebuild failed but continuing'",
    "prebuild": "rm -rf node_modules package-lock.json || true",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "bus",
    "tracking",
    "api",
    "metrobus",
    "stjohns"
  ],
  "author": "Your Name",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "better-sqlite3": "^12.2.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}